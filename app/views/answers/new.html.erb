<h2><%= @question.subject.name.join(" ") %>:</h2>

<h2><%= link_to @question.question_text, @question %></h2>

<%= form_for [@question, @answer] do |f| %>
  <%= f.text_field :body %>

  <%= f.submit %>
<% end %>
